# Simple Nginx-based frontend without Vite
FROM nginx:alpine

# Copy source files directly
COPY src /usr/share/nginx/html

# Copy nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose port
EXPOSE 8080

CMD ["nginx", "-g", "daemon off;"]
